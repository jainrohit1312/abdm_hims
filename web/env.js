// Runtime environment for Flutter web (window._env_).
//
// COMMITTED AS AN EMPTY TEMPLATE — do not put real secrets in this file.
// The Vercel build script (package.json → `vercel-build`) regenerates this
// file from the project environment variables during every deployment.
//
// Empty strings are treated as "not configured" by
// lib/config/supabase_web_config.dart, which then falls back to the
// --dart-define values and finally the local-development constants.
window._env_ = window._env_ || {};
window._env_.SUPABASE_URL = "";
window._env_.SUPABASE_ANON_KEY = "";
window._env_.DEEPSEEK_API_KEY = "";
